{
  "private": true,
  "scripts": {
    "build": "rimraf dist && cd web-tic && pnpm build && cd .. && pnpm -r run build"
  },
  "devDependencies": {
    "@iconify-json/mdi": "^1.0.14",
    "@slidev/cli": "^0.28.2",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*",
    "playwright-chromium": "^1.18.0",
    "pnpm": "^6.28.0",
    "rimraf": "^3.0.2"
  }
}
